host    replication     replicator      192.168.56.12/24       md5
host    replication     replicator      192.168.56.13/24       md5

pg_basebackup -h 192.168.56.11 -D /var/lib/postgresql/14/main -U replicator -P -v -R -X stream -C -S slave_1
pg_basebackup -h 192.168.56.11 -D /var/lib/postgresql/14/main -U replicator -P -v -R -X stream -C -S slave_2


# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     peer
# IPv4 local connections:
host    all             all             0.0.0.0/0               scram-sha-256
# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256
# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     peer
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256